import SwiftUI

struct ExpensesView: View {
    var body: some View {
        NavigationStack {
            Text("Expenses")
                .font(.largeTitle)
                .navigationTitle("Expenses")
        }
    }
}
