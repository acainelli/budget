---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BudgetApp.xcodeproj/project.pbxproj
  - BudgetApp/BudgetApp.entitlements
  - BudgetApp/Info.plist
  - BudgetApp/BudgetAppApp.swift
  - BudgetApp/Core/Models/Expense.swift
  - BudgetApp/Core/Models/MonthlyBudget.swift
  - BudgetAppTests/ExpenseModelTests.swift
  - BudgetAppTests/MonthlyBudgetTests.swift
  - BudgetAppTests/ExpenseCategoryTests.swift
  - BudgetAppTests/ContainerTests.swift
  - BudgetAppTests/FormatterTests.swift
  - BudgetAppTests/DesignSystemTests.swift
  - BudgetAppTests/HapticManagerTests.swift
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-06
  - APP-03

must_haves:
  truths:
    - "xcodebuild build -scheme BudgetApp exits 0 (project compiles without errors)"
    - "Expense can be instantiated with an amount, category, date, and notes; monthYear is stored as 'yyyy-MM' string set in init, never computed"
    - "MonthlyBudget cannot use @Attribute(.unique); fetch-or-create returns the same record on second call for the same monthYear"
    - "All 8 ExpenseCategory cases each have a non-empty symbol string and a non-nil SwiftUI Color"
    - "ModelContainer initialises successfully with cloudKitContainerIdentifier 'iCloud.com.placeholder.BudgetApp'"
    - "All 7 XCTest test files exist in BudgetAppTests/ and are referenced in project.pbxproj"
    - "BudgetApp.entitlements declares com.apple.developer.icloud-container-identifiers and com.apple.developer.icloud-services"
  artifacts:
    - path: "BudgetApp.xcodeproj/project.pbxproj"
      provides: "Xcode project file with BudgetApp target, BudgetAppTests target, entitlements reference, iOS 17 deployment target"
    - path: "BudgetApp/Core/Models/Expense.swift"
      provides: "Expense @Model class + ExpenseCategory enum with all 8 cases"
      exports: ["Expense", "ExpenseCategory"]
    - path: "BudgetApp/Core/Models/MonthlyBudget.swift"
      provides: "MonthlyBudget @Model class — no @Attribute(.unique)"
      exports: ["MonthlyBudget"]
    - path: "BudgetApp/BudgetAppApp.swift"
      provides: "@main entry point with CloudKit-backed ModelContainer"
      exports: ["BudgetAppApp"]
    - path: "BudgetApp/BudgetApp.entitlements"
      provides: "iCloud container and CloudKit capability declarations"
    - path: "BudgetAppTests/FormatterTests.swift"
      provides: "Unit test: 45.8.formattedEUR == '€45,80'"
  key_links:
    - from: "BudgetApp.xcodeproj/project.pbxproj"
      to: "BudgetApp/BudgetApp.entitlements"
      via: "CODE_SIGN_ENTITLEMENTS build setting"
      pattern: "CODE_SIGN_ENTITLEMENTS"
    - from: "BudgetApp/BudgetAppApp.swift"
      to: "BudgetApp/Core/Models/Expense.swift"
      via: "ModelContainer(for: Expense.self, MonthlyBudget.self)"
      pattern: "ModelContainer"
    - from: "project.pbxproj"
      to: "BudgetAppTests/"
      via: "PBXNativeTarget BudgetAppTests with file references"
      pattern: "BudgetAppTests"
---

<objective>
Create the complete Xcode project skeleton that compiles for iOS 17, write the SwiftData data models with full CloudKit compatibility, and lay down all XCTest stub files (Wave 0) before any feature code is added. This is the foundation every subsequent plan builds on.

Purpose: No other plan can run until a compilable .xcodeproj exists with the data models and test infrastructure in place.
Output: BudgetApp.xcodeproj (compilable), Expense.swift, MonthlyBudget.swift, BudgetAppApp.swift, BudgetApp.entitlements, and 7 XCTest stub files that compile (tests may be failing — that is expected).
</objective>

<execution_context>
@/Users/a.cainelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.cainelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project skeleton with test target</name>
  <files>
    BudgetApp.xcodeproj/project.pbxproj,
    BudgetApp/Info.plist,
    BudgetApp/BudgetApp.entitlements,
    BudgetApp/Assets.xcassets/Contents.json,
    BudgetApp/Assets.xcassets/AccentColor.colorset/Contents.json,
    BudgetApp/Assets.xcassets/AppIcon.appiconset/Contents.json,
    BudgetAppTests/BudgetAppTests.swift
  </files>
  <action>
    Create the full Xcode project directory structure from scratch. All GUIDs must be valid 24-character uppercase hex strings. Use the following concrete GUID values throughout the pbxproj (do NOT vary them — executor must use these exactly to ensure cross-file consistency):

    GUID assignments (use these verbatim):
    - Project object: AA000000000000000000001
    - BudgetApp target: AA000000000000000000002
    - BudgetAppTests target: AA000000000000000000003
    - main group: AA000000000000000000004
    - Products group: AA000000000000000000005
    - BudgetApp.app product ref: AA000000000000000000006
    - BudgetAppTests.xctest product ref: AA000000000000000000007
    - BudgetApp folder group: AA000000000000000000008
    - Core folder group: AA000000000000000000009
    - Models folder group: AA00000000000000000000A
    - Extensions folder group: AA00000000000000000000B
    - DesignSystem folder group: AA00000000000000000000C
    - Features folder group: AA00000000000000000000D
    - Dashboard folder group: AA00000000000000000000E
    - Expenses folder group: AA00000000000000000000F
    - Insights folder group: AA000000000000000000010
    - Settings folder group: AA000000000000000000011
    - AddExpense folder group: AA000000000000000000012
    - BudgetAppTests folder group: AA000000000000000000013
    - Assets.xcassets file ref: AA000000000000000000014
    - Info.plist file ref: AA000000000000000000015
    - BudgetApp.entitlements file ref: AA000000000000000000016
    - BudgetAppApp.swift file ref: AA000000000000000000017
    - ContentView.swift file ref: AA000000000000000000018
    - Expense.swift file ref: AA000000000000000000019
    - MonthlyBudget.swift file ref: AA00000000000000000001A
    - NumberFormatter+Currency.swift file ref: AA00000000000000000001B
    - DesignTokens.swift file ref: AA00000000000000000001C
    - CardStyle.swift file ref: AA00000000000000000001D
    - HapticManager.swift file ref: AA00000000000000000001E
    - CategoryIconView.swift file ref: AA00000000000000000001F
    - DashboardView.swift file ref: AA000000000000000000020
    - ExpensesView.swift file ref: AA000000000000000000021
    - InsightsView.swift file ref: AA000000000000000000022
    - SettingsView.swift file ref: AA000000000000000000023
    - AddExpenseView.swift file ref: AA000000000000000000024
    - ExpenseModelTests.swift file ref: AA000000000000000000025
    - MonthlyBudgetTests.swift file ref: AA000000000000000000026
    - ExpenseCategoryTests.swift file ref: AA000000000000000000027
    - ContainerTests.swift file ref: AA000000000000000000028
    - FormatterTests.swift file ref: AA000000000000000000029
    - DesignSystemTests.swift file ref: AA00000000000000000002A
    - HapticManagerTests.swift file ref: AA00000000000000000002B
    - BudgetAppTests.swift file ref: AA00000000000000000002C
    - Build file refs for app sources: increment from AA000000000000000000100
    - Build file refs for test sources: increment from AA000000000000000000200
    - App build config list: AA000000000000000000300
    - Tests build config list: AA000000000000000000301
    - Project build config list: AA000000000000000000302
    - Debug config (project): AA000000000000000000303
    - Release config (project): AA000000000000000000304
    - Debug config (app): AA000000000000000000305
    - Release config (app): AA000000000000000000306
    - Debug config (tests): AA000000000000000000307
    - Release config (tests): AA000000000000000000308

    **project.pbxproj content requirements:**
    - archiveVersion = 1; objectVersion = 77; (Xcode 15 compatible)
    - IPHONEOS_DEPLOYMENT_TARGET = 17.0 for all configurations
    - SWIFT_VERSION = 5.0
    - PRODUCT_BUNDLE_IDENTIFIER = com.placeholder.BudgetApp (app target)
    - PRODUCT_BUNDLE_IDENTIFIER = com.placeholder.BudgetApp.Tests (tests target)
    - CODE_SIGN_ENTITLEMENTS = BudgetApp/BudgetApp.entitlements (in app target Debug + Release build settings)
    - INFOPLIST_FILE = BudgetApp/Info.plist
    - PRODUCT_NAME = BudgetApp (app target) / BudgetAppTests (tests target)
    - TEST_HOST = $(BUILT_PRODUCTS_DIR)/BudgetApp.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/BudgetApp (tests target)
    - BUNDLE_LOADER = $(TEST_HOST) (tests target)
    - Include ALL source file build phases: every Swift file listed in the GUID table above must appear in a PBXBuildFile entry AND in the correct PBXSourcesBuildPhase
    - App target sources build phase: BudgetAppApp.swift, ContentView.swift, Expense.swift, MonthlyBudget.swift, NumberFormatter+Currency.swift, DesignTokens.swift, CardStyle.swift, HapticManager.swift, CategoryIconView.swift, DashboardView.swift, ExpensesView.swift, InsightsView.swift, SettingsView.swift, AddExpenseView.swift
    - Tests target sources build phase: BudgetAppTests.swift, ExpenseModelTests.swift, MonthlyBudgetTests.swift, ExpenseCategoryTests.swift, ContainerTests.swift, FormatterTests.swift, DesignSystemTests.swift, HapticManagerTests.swift
    - App target resources build phase: Assets.xcassets
    - PBXFrameworksBuildPhase for tests target: must link the app's test host via DependenciesBuildPhase — add BudgetApp.app as a dependency (PBXTargetDependency) to the test target

    **BudgetApp/Info.plist:**
    Standard iOS app Info.plist. Include:
    - CFBundleDevelopmentRegion = en
    - CFBundleExecutable = $(EXECUTABLE_NAME)
    - CFBundleIdentifier = $(PRODUCT_BUNDLE_IDENTIFIER)
    - CFBundleInfoDictionaryVersion = 6.0
    - CFBundleName = $(PRODUCT_NAME)
    - CFBundlePackageType = APPL
    - CFBundleShortVersionString = 1.0
    - CFBundleVersion = 1
    - LSRequiresIPhoneOS = true
    - UILaunchScreen = {} (empty dict)
    - UIRequiredDeviceCapabilities = [armv7]
    - UISupportedInterfaceOrientations = [UIInterfaceOrientationPortrait]
    - UIBackgroundModes = [remote-notification]

    **BudgetApp/BudgetApp.entitlements:**
    XML plist with:
    - com.apple.developer.icloud-container-identifiers = [iCloud.com.placeholder.BudgetApp]
    - com.apple.developer.icloud-services = [CloudKit]
    - com.apple.developer.ubiquity-kvstore-identifier = $(TeamIdentifierPrefix)com.placeholder.BudgetApp

    **BudgetApp/Assets.xcassets/Contents.json:**
    {"info":{"author":"xcode","version":1}}

    **BudgetApp/Assets.xcassets/AccentColor.colorset/Contents.json:**
    Color #004225 in sRGB: R=0.000, G=0.259, B=0.145, A=1.000
    Use universal idiom, srgb color-space. See RESEARCH.md for exact JSON structure.

    **BudgetApp/Assets.xcassets/AppIcon.appiconset/Contents.json:**
    Single universal 1024x1024 entry pointing to AppIcon.png (the PNG will be added in Plan 02 — for now the JSON must exist and reference the filename):
    {"images":[{"filename":"AppIcon.png","idiom":"universal","platform":"ios","size":"1024x1024"}],"info":{"author":"xcode","version":1}}

    **BudgetAppTests/BudgetAppTests.swift:**
    Minimal XCTest file that compiles:
    ```swift
    import XCTest
    @testable import BudgetApp

    final class BudgetAppTests: XCTestCase {}
    ```
  </action>
  <verify>
    <automated>xcodebuild build -scheme BudgetApp -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -project /Users/a.cainelli/budget/BudgetApp.xcodeproj 2>&1 | tail -5</automated>
  </verify>
  <done>
    - BudgetApp.xcodeproj/project.pbxproj exists and references all source files listed above
    - BudgetApp/BudgetApp.entitlements exists with CloudKit container identifier
    - BudgetApp/Info.plist exists with UIBackgroundModes including remote-notification
    - Assets.xcassets directory and AccentColor.colorset exist
    - BudgetAppTests/BudgetAppTests.swift exists
    - xcodebuild build exits 0 (may fail until Swift source files are created in Task 2 — that is acceptable; the project structure must be valid)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SwiftData models — Expense, ExpenseCategory, MonthlyBudget, App entry point</name>
  <files>
    BudgetApp/BudgetAppApp.swift,
    BudgetApp/ContentView.swift,
    BudgetApp/Core/Models/Expense.swift,
    BudgetApp/Core/Models/MonthlyBudget.swift
  </files>
  <behavior>
    - Expense(amount:45.8, category:.groceries, date:Date(), notes:"test") sets monthYear to current "yyyy-MM" string (never a computed property)
    - Expense.monthYear equals DateFormatter("yyyy-MM").string(from: expense.date) — verified in ExpenseModelTests
    - MonthlyBudget(monthYear:"2026-03") creates a record with income defaulting to 0.0
    - MonthlyBudget has NO @Attribute(.unique) — uniqueness enforced by fetch-or-create at call sites
    - All 8 ExpenseCategory cases exist; each has a non-empty symbol string and non-nil color
    - ModelContainer(for: Expense.self, MonthlyBudget.self, configurations: ModelConfiguration(isStoredInMemoryOnly: true)) does NOT throw
    - @main struct BudgetAppApp creates ModelContainer with cloudKitContainerIdentifier "iCloud.com.placeholder.BudgetApp"; fatalError on failure
  </behavior>
  <action>
    Write these four files exactly as described. No placeholder stubs — full compilable Swift code.

    **BudgetApp/Core/Models/Expense.swift:**
    - import SwiftData, Foundation, SwiftUI
    - @Model final class Expense with properties: amount: Double, category: ExpenseCategory, date: Date, notes: String = "", monthYear: String
    - init sets monthYear using DateFormatter with format "yyyy-MM" applied to the date argument — this is a STORED property, NOT computed
    - ExpenseCategory enum: String, CaseIterable, Codable — all 8 cases (groceries, restaurants, car, mealVoucher, pharmacy, bills, chico, shopping)
    - Each case has `var symbol: String` switch and `var color: Color` switch using system colors per CONTEXT.md locked decisions
    - chico uses Color(uiColor: .brown) (not Color.brown which does not exist in all contexts)
    - `var displayName: String { rawValue.capitalized }` — but mealVoucher.displayName will be "Mealvoucher"; that is acceptable for Phase 1
    - Include CategoryIconView in this file or a separate file? Separate: create BudgetApp/Core/DesignSystem/CategoryIconView.swift in Plan 02. For now, only the enum goes here.

    **BudgetApp/Core/Models/MonthlyBudget.swift:**
    - import SwiftData
    - @Model final class MonthlyBudget with: monthYear: String, income: Double = 0
    - init(monthYear: String, income: Double = 0)
    - DO NOT add @Attribute(.unique) — CloudKit incompatibility (see RESEARCH.md Pitfall 1)
    - Include fetchOrCreate static helper function:
      ```swift
      static func fetchOrCreate(monthYear: String, in context: ModelContext) -> MonthlyBudget {
          let descriptor = FetchDescriptor<MonthlyBudget>(
              predicate: #Predicate { $0.monthYear == monthYear }
          )
          if let existing = try? context.fetch(descriptor).first {
              return existing
          }
          let budget = MonthlyBudget(monthYear: monthYear)
          context.insert(budget)
          try? context.save()
          return budget
      }
      ```

    **BudgetApp/BudgetAppApp.swift:**
    - @main struct BudgetAppApp: App
    - let container: ModelContainer
    - init() creates ModelContainer(for: Expense.self, MonthlyBudget.self, configurations: ModelConfiguration(cloudKitContainerIdentifier: "iCloud.com.placeholder.BudgetApp"))
    - fatalError on catch
    - body: WindowGroup { ContentView().preferredColorScheme(.dark) }.modelContainer(container)
    - In DEBUG builds, include a commented-out call: // try? container.initializeCloudKitSchema()

    **BudgetApp/ContentView.swift:**
    - Minimal placeholder so the project compiles: struct ContentView: View { var body: some View { Text("BudgetApp") } }
    - This will be replaced in Plan 02 Task 2. For now it just needs to compile.
  </action>
  <verify>
    <automated>xcodebuild build -scheme BudgetApp -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -project /Users/a.cainelli/budget/BudgetApp.xcodeproj 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Expense.swift compiles with @Model annotation, all 8 ExpenseCategory cases, monthYear stored property
    - MonthlyBudget.swift compiles with @Model annotation, NO @Attribute(.unique), fetchOrCreate helper
    - BudgetAppApp.swift compiles and sets up CloudKit-backed ModelContainer
    - xcodebuild build exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 — Write all XCTest stub files (failing tests allowed)</name>
  <files>
    BudgetAppTests/ExpenseModelTests.swift,
    BudgetAppTests/MonthlyBudgetTests.swift,
    BudgetAppTests/ExpenseCategoryTests.swift,
    BudgetAppTests/ContainerTests.swift,
    BudgetAppTests/FormatterTests.swift,
    BudgetAppTests/DesignSystemTests.swift,
    BudgetAppTests/HapticManagerTests.swift
  </files>
  <behavior>
    - ExpenseModelTests: testMonthYearStoredCorrectly verifies expense.monthYear equals DateFormatter("yyyy-MM").string(from: date)
    - MonthlyBudgetTests: testFetchOrCreateIdempotency verifies two calls with same monthYear return the same record (same object identity using in-memory container)
    - ExpenseCategoryTests: testAllCategoriesHaveSymbol iterates ExpenseCategory.allCases and asserts !category.symbol.isEmpty for each; testAllCategoriesHaveColor does the same asserting color is not Color.clear
    - ContainerTests: testContainerInitWithInMemoryConfig verifies ModelContainer(for: Expense.self, MonthlyBudget.self, configurations: ModelConfiguration(isStoredInMemoryOnly: true)) does not throw — uses in-memory, NOT CloudKit identifier (tests run without iCloud)
    - FormatterTests: testEURGermanFormat asserts 45.8.formattedEUR == "€45,80" — this test MUST use the exact string "€45,80" (comma decimal, euro prefix); if the formatter produces suffix euro sign, the test fails and the formatter must be fixed
    - DesignSystemTests: testCardStyleTokens asserts DesignTokens.CornerRadius.card == 20 and DesignTokens.CornerRadius.small == 12 and DesignTokens.Padding.outer == 16 and DesignTokens.Padding.inner == 8; testCardStyleModifierExists verifies a simple View can have .cardStyle() called on it (compile-time test via XCTAssertNoThrow on a view builder)
    - HapticManagerTests: testImpactDoesNotCrash, testSuccessDoesNotCrash, testWarningDoesNotCrash, testSelectionDoesNotCrash — each just calls the static method; no crash = pass
  </behavior>
  <action>
    Write all 7 test files. Tests must compile even if they call APIs that don't fully exist yet (DesignTokens, NumberFormatter+Currency.eurGerman, HapticManager). Use @testable import BudgetApp in all files.

    The FormatterTests assertion `45.8.formattedEUR == "€45,80"` is the critical test for DATA-05. If the de_DE locale produces "45,80 €" (suffix), the formatter implementation in Plan 02 must apply a format override. Write the test expecting "€45,80" — if it fails, fix the formatter in Task 1 of Plan 02.

    Each file follows this structure:
    ```swift
    import XCTest
    import SwiftData
    @testable import BudgetApp

    final class XxxTests: XCTestCase {
        // helper to create in-memory container
        func makeContainer() throws -> ModelContainer {
            let config = ModelConfiguration(isStoredInMemoryOnly: true)
            return try ModelContainer(for: Expense.self, MonthlyBudget.self,
                                      configurations: config)
        }

        func testXxx() throws { ... }
    }
    ```

    DesignSystemTests and HapticManagerTests do not need a container helper.

    Write tests that will PASS once Plan 02 implements DesignTokens and HapticManager. The tests compile now because the type names are declared — even if the files don't exist yet, write the tests referencing what WILL exist. The test target will not compile until Plan 02 completes; that is acceptable per Wave 0 protocol.

    Actually: tests MUST compile at the end of Plan 01 execution, because xcodebuild test will be run. Therefore: if DesignTokens, HapticManager, NumberFormatter+Currency are not yet written (they are Plan 02), the test files that reference them will cause compile errors. Resolution: create minimal stub versions of these types in Plan 01 so tests compile, and replace with full implementations in Plan 02.

    Create these stubs in the test files themselves using #if DEBUG guards, OR create minimal stub source files:
    - BudgetApp/Core/Extensions/NumberFormatter+Currency.swift — stub with formattedEUR returning "" initially? No — write the real implementation here since it is a pure function with no dependencies.
    - BudgetApp/Core/DesignSystem/DesignTokens.swift — stub with the real constants (trivial, no dependencies)
    - BudgetApp/Core/DesignSystem/HapticManager.swift — stub with the real implementation (UIKit-only, no SwiftData dependency)

    Write the FULL implementations of NumberFormatter+Currency.swift, DesignTokens.swift, and HapticManager.swift in this task (they are pure utility files with no dependency on Plan 02 SwiftUI views). Plan 02 will add CardStyle.swift and CategoryIconView.swift which are the only remaining design system files.

    **BudgetApp/Core/Extensions/NumberFormatter+Currency.swift:**
    ```swift
    import Foundation

    extension NumberFormatter {
        static let eurGerman: NumberFormatter = {
            let f = NumberFormatter()
            f.numberStyle = .currency
            f.locale = Locale(identifier: "de_DE")
            f.currencyCode = "EUR"
            // Force euro sign to prefix position per spec ("€45,80" not "45,80 €")
            f.positiveFormat = "€#,##0.00"
            f.negativeFormat = "-€#,##0.00"
            return f
        }()
    }

    extension Double {
        var formattedEUR: String {
            NumberFormatter.eurGerman.string(from: NSNumber(value: self)) ?? "€0,00"
        }
    }
    ```
    Note: The positiveFormat/negativeFormat override forces prefix euro sign. The test testEURGermanFormat MUST assert "€45,80" — validate this is correct by tracing: 45.8 with format "€#,##0.00" and de_DE locale → "€45,80". If the comma is not produced by de_DE grouping separator in this format, use "€#.##0,00" style — but de_DE uses comma as decimal and period as group separator, so #,##0.00 in de_DE produces commas for decimals. Verify with the test.

    **BudgetApp/Core/DesignSystem/DesignTokens.swift:**
    ```swift
    import CoreGraphics

    enum DesignTokens {
        enum CornerRadius {
            static let card: CGFloat = 20
            static let small: CGFloat = 12
            static let categoryIcon: CGFloat = 8
        }
        enum Padding {
            static let outer: CGFloat = 16
            static let inner: CGFloat = 8
        }
        enum FAB {
            static let size: CGFloat = 56
        }
    }
    ```

    **BudgetApp/Core/DesignSystem/HapticManager.swift:**
    ```swift
    import UIKit

    struct HapticManager {
        static func impact() {
            UIImpactFeedbackGenerator(style: .medium).impactOccurred()
        }
        static func success() {
            UINotificationFeedbackGenerator().notificationOccurred(.success)
        }
        static func warning() {
            UINotificationFeedbackGenerator().notificationOccurred(.warning)
        }
        static func selection() {
            UISelectionFeedbackGenerator().selectionChanged()
        }
    }
    ```

    These three files must also be listed in the project.pbxproj build phases (app target sources). If Task 1 already included them in the GUID table and build phases, they will compile automatically. If they were omitted from Task 1's pbxproj, add their file references and build file entries to project.pbxproj now.

    After writing all 7 test files + the 3 utility source files, run xcodebuild test to verify the test target compiles. Tests may fail (RED phase is acceptable for formatterTests if the format string needs tuning). Fix FormatterTests until it passes GREEN before committing.
  </action>
  <verify>
    <automated>xcodebuild test -scheme BudgetApp -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -project /Users/a.cainelli/budget/BudgetApp.xcodeproj -only-testing BudgetAppTests/FormatterTests -only-testing BudgetAppTests/ExpenseCategoryTests -only-testing BudgetAppTests/DesignSystemTests -only-testing BudgetAppTests/HapticManagerTests 2>&1 | grep -E "TEST (PASSED|FAILED)|Build Succeeded|Build Failed"</automated>
  </verify>
  <done>
    - All 7 test files exist in BudgetAppTests/
    - NumberFormatter+Currency.swift, DesignTokens.swift, HapticManager.swift exist in Core/
    - xcodebuild test compiles without errors
    - FormatterTests, ExpenseCategoryTests, DesignSystemTests, HapticManagerTests pass (GREEN)
    - ExpenseModelTests, MonthlyBudgetTests, ContainerTests compile (may be incomplete stubs until models are finalized — they compile)
  </done>
</task>

</tasks>

<verification>
Full build check after all tasks:
```
xcodebuild build -scheme BudgetApp -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -project /Users/a.cainelli/budget/BudgetApp.xcodeproj
```

Partial test suite (tests that can run at end of Plan 01):
```
xcodebuild test -scheme BudgetApp -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -project /Users/a.cainelli/budget/BudgetApp.xcodeproj
```
</verification>

<success_criteria>
- xcodebuild build exits 0 on the BudgetApp scheme
- BudgetApp.xcodeproj/project.pbxproj contains both BudgetApp and BudgetAppTests targets
- Expense.swift has @Model, stored monthYear, all 8 ExpenseCategory cases with symbol + color
- MonthlyBudget.swift has @Model, no @Attribute(.unique), fetchOrCreate helper
- BudgetApp.entitlements has iCloud container identifier
- All 7 XCTest files exist and the test target compiles
- FormatterTests passes: 45.8.formattedEUR == "€45,80"
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
