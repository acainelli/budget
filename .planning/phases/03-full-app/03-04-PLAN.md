---
phase: 03-full-app
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-05, INS-01, INS-02, INS-03, INS-04, INS-05, INS-06, SET-01, SET-02, SET-03, SET-04, SET-05]

must_haves:
  truths:
    - "All four tabs are fully functional end-to-end"
    - "Expenses tab: grouped list, search, detail/edit, bulk delete all work"
    - "Insights tab: pie/trend/comparison charts and heat-map calendar all render"
    - "Settings tab: income saves, export produces JSON, import works, delete all works"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all three newly built tabs working together in the complete 4-tab app.

Purpose: Confirm all Phase 3 features are visually correct and functionally working before marking the phase complete.
Output: Verified working app.
</objective>

<execution_context>
@/Users/a.cainelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.cainelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-full-app/03-01-SUMMARY.md
@.planning/phases/03-full-app/03-02-SUMMARY.md
@.planning/phases/03-full-app/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all Phase 3 tabs end-to-end</name>
  <files></files>
  <action>
    Human verifies all three newly built tabs in the simulator. This is a checkpoint — no code changes, only verification.

    Open the project in Xcode and run on a simulator (Cmd+R). Then test each flow:

    **Expenses Tab (tab 2):**
    1. Add a few expenses via the FAB if none exist
    2. Verify expenses appear grouped by day with daily totals in section headers
    3. Search for an expense by notes text — verify filtering works
    4. Search by category name (e.g., "groceries") — verify filtering works
    5. Tap an expense row — verify detail view opens with category, amount (read-only), date picker, notes field
    6. Edit the date to a different month — verify the expense moves to the correct month group
    7. Swipe left on a row — verify delete works
    8. Tap the trash icon in the toolbar — verify bulk delete sheet opens with Day/Week/Month picker and preview count
    9. Select a scope and confirm deletion — verify expenses are removed

    **Insights Tab (tab 3):**
    10. Verify segmented picker shows Charts / Calendar
    11. In Charts: verify pie chart (donut), spending trend line chart, and 6-month comparison bar chart all render
    12. Change month via the picker — verify charts update
    13. Switch to Calendar — verify heat-map grid with accent-green opacity
    14. Tap a day with expenses — verify sheet shows that day's expenses
    15. Switch to week view — verify horizontal 7-day row with left/right navigation

    **Settings Tab (tab 4):**
    16. Enter a monthly income value and save — verify it persists (go to Dashboard and check if income shows)
    17. Tap Data Management — verify export/import/delete options appear
    18. Tap Export — verify share sheet appears with JSON file
    19. Tap Delete All Data — verify confirmation dialog, then confirm and verify all data is removed

    Resume signal: Type "approved" if all flows work, or describe any issues found.
  </action>
  <verify>Human confirms all flows work on simulator</verify>
  <done>All 5 phase success criteria verified by human tester: grouped expenses with search, bulk delete, insights charts and calendar, settings with income/export/import/delete-all.</done>
</task>

</tasks>

<verification>
Human confirms all four tabs work end-to-end on simulator.
</verification>

<success_criteria>
All 5 phase success criteria verified by human tester.
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-app/03-04-SUMMARY.md`
</output>
