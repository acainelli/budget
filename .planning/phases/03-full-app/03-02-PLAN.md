---
phase: 03-full-app
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - BudgetApp/Features/Insights/InsightsView.swift
  - BudgetApp/Features/Insights/InsightsChartsView.swift
  - BudgetApp/Features/Insights/SpendingTrendChart.swift
  - BudgetApp/Features/Insights/MonthlyComparisonChart.swift
  - BudgetApp/Features/Insights/InsightsCalendarView.swift
  - BudgetApp/Features/Insights/CalendarWeekView.swift
  - BudgetApp/Features/Insights/DayExpensesSheet.swift
  - BudgetApp.xcodeproj/project.pbxproj
autonomous: true
requirements: [INS-01, INS-02, INS-03, INS-04, INS-05, INS-06]

must_haves:
  truths:
    - "User sees segmented picker (Charts / Calendar) at top of Insights tab"
    - "Charts view shows category pie chart, spending trend line chart, and 6-month comparison bar chart"
    - "Charts view shows ALL expenses without filtering and has no own NavigationStack"
    - "Calendar month view shows 7-column heat-map grid with accent color at varying opacity"
    - "Tapping a calendar day opens a sheet with that day's expenses"
    - "Calendar week view shows horizontal 7-day row with left/right navigation"
  artifacts:
    - path: "BudgetApp/Features/Insights/InsightsView.swift"
      provides: "Container with segmented picker and shared monthYear state"
      min_lines: 40
    - path: "BudgetApp/Features/Insights/InsightsChartsView.swift"
      provides: "Pie chart, trend chart, and comparison chart in scrollable layout"
      min_lines: 30
    - path: "BudgetApp/Features/Insights/SpendingTrendChart.swift"
      provides: "LineMark daily spending trend for selected month"
      min_lines: 30
    - path: "BudgetApp/Features/Insights/MonthlyComparisonChart.swift"
      provides: "BarMark last 6 months total spending comparison"
      min_lines: 30
    - path: "BudgetApp/Features/Insights/InsightsCalendarView.swift"
      provides: "Month calendar grid with heat-map opacity and week view toggle"
      min_lines: 80
    - path: "BudgetApp/Features/Insights/DayExpensesSheet.swift"
      provides: "Sheet showing flat list of expenses for a tapped day"
      min_lines: 30
  key_links:
    - from: "InsightsView.swift"
      to: "InsightsChartsView / InsightsCalendarView"
      via: "Segmented picker switching between views"
      pattern: "Picker.*Charts.*Calendar"
    - from: "InsightsChartsView.swift"
      to: "SpendingTrendChart / MonthlyComparisonChart"
      via: "Composed in VStack/ScrollView"
      pattern: "SpendingTrendChart|MonthlyComparisonChart"
    - from: "InsightsCalendarView.swift"
      to: "DayExpensesSheet"
      via: "Day cell tap triggers sheet"
      pattern: "sheet.*DayExpensesSheet"
---

<objective>
Build the complete Insights tab: Charts view with pie chart (reusing donut pattern), spending trend line chart, 6-month comparison bar chart; Calendar view with heat-map month grid and week view.

Purpose: Users need visual spending analysis beyond the dashboard to understand trends and patterns.
Output: Seven Swift files replacing the InsightsView stub, registered in pbxproj.
</objective>

<execution_context>
@/Users/a.cainelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.cainelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-app/03-CONTEXT.md

<interfaces>
<!-- Reuse donut chart pattern from Dashboard -->

From BudgetApp/Features/Dashboard/CategoryBreakdownCard.swift:
```swift
// SectorMark donut pattern to replicate for Insights pie chart:
Chart(categoryTotals) { item in
    SectorMark(angle: .value("Amount", item.total), innerRadius: .ratio(0.6), angularInset: 1.5)
    .foregroundStyle(item.category.color)
}
// Legend pattern: ForEach with Circle color dot + displayName + formattedEUR
```

From BudgetApp/Features/Dashboard/MonthPickerView.swift:
```swift
struct MonthPickerView: View {
    @Binding var currentMonth: Date
    // Chevron nav, drag gesture, Today pill — reusable for Insights
}
```

From BudgetApp/Core/Models/Expense.swift:
```swift
@Model final class Expense {
    var amount: Double, var category: ExpenseCategory, var date: Date
    var notes: String, var monthYear: String
}
```

Established patterns:
- @Query all expenses, in-memory filter by monthYear
- .cardStyle() for card containers
- HapticManager for interactions
- DesignTokens for spacing/corner radius
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InsightsView container, ChartsView with pie/trend/comparison charts</name>
  <files>BudgetApp/Features/Insights/InsightsView.swift, BudgetApp/Features/Insights/InsightsChartsView.swift, BudgetApp/Features/Insights/SpendingTrendChart.swift, BudgetApp/Features/Insights/MonthlyComparisonChart.swift</files>
  <action>
Replace InsightsView.swift stub:
- @Query(sort: \Expense.date) var allExpenses: [Expense]
- @State var currentMonth: Date = Date() (first of current month, same pattern as DashboardView)
- @State var selectedSegment: InsightsSegment = .charts (enum with cases .charts, .calendar)
- NO NavigationStack here (per INS-03 — the parent tab already has NavigationStack from ContentView pattern... actually, check: the stub currently HAS a NavigationStack. Keep it for the navigationTitle, but the charts/calendar content should not add another one)
- Keep the NavigationStack wrapper for .navigationTitle("Insights")
- VStack: MonthPickerView(currentMonth: $currentMonth), Picker for segment (.pickerStyle(.segmented)), then switch on selectedSegment to show InsightsChartsView or InsightsCalendarView
- Both child views receive allExpenses and currentMonth
- Compute currentMonthYear string from currentMonth using DateFormatter("yyyy-MM")
- Filter monthExpenses = allExpenses.filter { $0.monthYear == currentMonthYear } for Charts view

Create InsightsChartsView.swift:
- Takes `expenses: [Expense]` (filtered to current month) and `allExpenses: [Expense]` (for 6-month comparison)
- Takes `currentMonth: Date` for the comparison chart date range
- ScrollView containing VStack(spacing: 16):
  1. Category pie chart card (reuse SectorMark donut pattern from CategoryBreakdownCard — slightly larger frame height: 250 instead of 200). Same CategoryTotal struct, same legend. Wrap in .cardStyle(). Title "Spending by Category"
  2. SpendingTrendChart(expenses: expenses, currentMonth: currentMonth) wrapped in .cardStyle()
  3. MonthlyComparisonChart(allExpenses: allExpenses, currentMonth: currentMonth) wrapped in .cardStyle()

Create SpendingTrendChart.swift:
- Takes `expenses: [Expense]` (current month) and `currentMonth: Date`
- Compute daily totals: group expenses by Calendar.current.startOfDay(for: expense.date), sum amounts
- Fill in zero for days with no spending (iterate 1...daysInMonth)
- Chart with LineMark(x: .value("Day", dayDate), y: .value("Amount", total))
  - .interpolationMethod(.catmullRom) for smooth line
  - .foregroundStyle(Color.accentColor)
- Add AreaMark with same data and .opacity(0.1) for subtle fill
- .chartYAxis with .formattedEUR abbreviated labels
- .frame(height: 200)
- Title "Spending Trend"

Create MonthlyComparisonChart.swift:
- Takes `allExpenses: [Expense]` and `currentMonth: Date`
- Compute last 6 months: iterate -5...0, compute monthYear strings, sum expenses per month
- Chart with BarMark(x: .value("Month", monthLabel), y: .value("Total", total))
  - monthLabel: short format "MMM" (Jan, Feb, etc.)
  - .foregroundStyle(Color.accentColor) for current month, .foregroundStyle(.secondary) for others
- .frame(height: 200)
- Title "6-Month Comparison"
  </action>
  <verify>
    <automated>cd /Users/a.cainelli/budget && xcodebuild build -project BudgetApp.xcodeproj -scheme BudgetApp -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <done>InsightsView shows segmented picker, Charts segment displays pie chart, spending trend LineMark, and 6-month comparison BarMark.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar heat-map view, week view, day detail sheet, and pbxproj registration</name>
  <files>BudgetApp/Features/Insights/InsightsCalendarView.swift, BudgetApp/Features/Insights/CalendarWeekView.swift, BudgetApp/Features/Insights/DayExpensesSheet.swift, BudgetApp.xcodeproj/project.pbxproj</files>
  <action>
Create InsightsCalendarView.swift:
- Takes `expenses: [Expense]` (filtered to current month) and `currentMonth: Date`
- @State var selectedDay: Date? = nil (for sheet)
- @State var viewMode: CalendarViewMode = .month (enum .month, .week)
- Segmented or toggle for month/week view mode (small, secondary)
- Month view: 7-column LazyVGrid with GridItem(.flexible()) x 7
  - Header row: weekday abbreviations (M, T, W, T, F, S, S)
  - Compute first day of month, leading blank cells for alignment (use Calendar.current.component(.weekday, from: firstOfMonth) adjusted for Monday-start if locale uses it)
  - Each day cell: day number text, background RoundedRectangle with accent color (#004225) at variable opacity
  - Opacity calculation: dailyTotal / maxDailyTotal for the month — no spending = 0 opacity (transparent), highest day = 1.0 (full accent color). Clamp to 0.1 minimum if any spending so it's visible
  - Cell size ~44x44, corner radius DesignTokens.CornerRadius.categoryIcon (8)
  - onTapGesture: set selectedDay, HapticManager.selection()
- Week view: CalendarWeekView(expenses: expenses, currentMonth: currentMonth)
- .sheet(item: $selectedDay) — wait, Date is not Identifiable. Use: .sheet(isPresented: binding derived from selectedDay != nil) { if let day = selectedDay { DayExpensesSheet(day: day, expenses: expensesForDay(day)) } }
  Actually, simpler: @State var showDaySheet = false, set both selectedDay and showDaySheet on tap

Create CalendarWeekView.swift (INS-06):
- Takes `expenses: [Expense]` and a `currentWeekStart: Date` binding or internal state
- @State var weekStart: Date initialized to start of current week
- Horizontal HStack showing 7 days (weekStart to weekStart+6):
  - Each day: VStack with weekday abbreviation, day number, small accent-colored circle indicator if any expenses that day
  - Similar heat-map opacity approach but in a row
- Left/right navigation buttons (chevron.left / chevron.right) to shift weekStart by +/- 7 days
- HapticManager.selection() on navigation

Create DayExpensesSheet.swift (INS-05):
- Takes `day: Date` and `expenses: [Expense]`
- NavigationStack with .navigationTitle(day formatted as "EEEE, d MMMM yyyy")
- Header: daily total in large .formattedEUR
- List of expenses: each row = CategoryIconView + category name + notes + amount (similar to ExpenseRowView but view-only, no navigation)
- .presentationDetents([.medium, .large])
- Toolbar dismiss button

Register ALL 6 new Insights files in project.pbxproj:
- InsightsChartsView.swift, SpendingTrendChart.swift, MonthlyComparisonChart.swift, InsightsCalendarView.swift, CalendarWeekView.swift, DayExpensesSheet.swift
- Follow established GUID pattern: file refs in 0x53-0x58 range, build file refs in 0x11A-0x11F range
- Add to PBXBuildFile, PBXFileReference, PBXGroup (Features/Insights children), PBXSourcesBuildPhase
- Note: InsightsView.swift already exists in pbxproj — only register the 6 new files
  </action>
  <verify>
    <automated>cd /Users/a.cainelli/budget && xcodebuild build -project BudgetApp.xcodeproj -scheme BudgetApp -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <done>Calendar segment shows heat-map month grid with tappable days opening day-detail sheet, and week view with left/right navigation. All 7 Insights files compile.</done>
</task>

</tasks>

<verification>
- xcodebuild BUILD SUCCEEDED
- InsightsView replaces stub with segmented Charts/Calendar picker
- Charts: pie chart (SectorMark donut), trend (LineMark), comparison (BarMark 6 months)
- Calendar month: 7-column grid, heat-map opacity from accent color
- Calendar day tap: sheet with day's expenses
- Calendar week: horizontal 7-day row with navigation
- All 7 files registered in pbxproj
</verification>

<success_criteria>
- Build succeeds with all Insights tab files
- INS-01: Segmented picker with shared monthYear state
- INS-02: Pie chart, trend chart, 6-month comparison
- INS-03: Charts view shows all expenses, no own NavigationStack (parent has one)
- INS-04: Heat-map calendar with accent color opacity
- INS-05: Tappable days open expense detail sheet
- INS-06: Week view with 7-day row and navigation
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-app/03-02-SUMMARY.md`
</output>
